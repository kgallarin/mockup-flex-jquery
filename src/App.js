$(document).ready(function(){
    $('#fullpage').fullpage({
        slidesNavigation: true, 
        lazyLoading: true,
        navigation:true
    });

    // $.fn.fullpage.setAllowScrolling(false);
})